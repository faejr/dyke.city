---
import { requestHasLocale } from "astro:i18n";
import Layout from "../layouts/Layout.astro";
import { createInviteCode } from "../handle";

const inviteCode = await createInviteCode();
---

<Layout title="dyke.city">
  <main>
    <img src="/assets/logo.png" />
    <p>{inviteCode.code}</p>
  </main>
</Layout>
